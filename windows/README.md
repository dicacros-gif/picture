# Picture Cleaner PC · 무인 CLI 블로그 (2026-09-12)

사진 정리·실시간 검색어·네이버 글쓰기·댓글 도구를 포함한 Windows 프로그램입니다.
실행 파일: `dist/PictureCleanerPC-Unattended-20260912.exe`
기존 프로그램을 닫고 새 파일을 실행하세요. 제목에 `무인 CLI 블로그 2026-09-12`가 표시됩니다.
**시작 시 자동 실행**은 기본으로 켜져 있습니다. 초기 실시간 조회가 브라우저 사용을 마치면
첫 회차를 바로 시작하며, 상단 체크박스를 끄면 다음 실행부터 직접 시작 버튼을 눌러 실행합니다.

## CLI 글쓰기 사용법

1. **3 네이버 블로그 자동화**에서 **CLI 연결 확인**을 누릅니다. 로그인되지 않은 CLI는
   **CLI 로그인 안내**에서 해당 로그인 창을 엽니다. ChatGPT는 Codex CLI의 ChatGPT 계정 로그인,
   Claude는 Claude 계정 로그인, Antigravity는 자체 로그인 세션을 사용합니다.
   작업 시작 전에도 필수 CLI 설치·로그인을 확인하므로, 필요한 로그인이 없으면 생성 전에 알려줍니다.
   미로그인 CLI가 있으면 자동화를 멈추고 **CLI 로그인 필요** 창을 표시합니다. 이 창은 다른
   프로그램 기능을 잠그지 않습니다. **지금 로그인**으로 콘솔에서 인증하고 콘솔을 닫으면 다시
   점검합니다. 필수 CLI가 모두 통과하면 중단했던 자동화를 바로 재개합니다.
   Antigravity는 별도 계정 조회 명령이 없어 재로그인 뒤 과거 인증 실패 기록을 해제하고,
   다음 실제 CLI 요청에서 계정을 확인합니다. 콘솔 종료만으로 로그인 성공이라고 표시하지 않습니다.
   **실행 순서 변경**으로 사용할 CLI를 직접 고르고 **프로그램 다시 시작**을 누를 수도 있습니다.
   프로그램은 순서를 임의로 대체하지 않습니다. 재시작은 현재 작업 중단과 설정 저장을 마친 후,
   이전 프로세스의 종료를 확인한 새 프로세스가 시작하도록 동기화합니다.
2. 주제를 직접 입력하거나 **관심 주제 자동 선정**을 누릅니다. 연관 검색어는 1번 탭에서 확인합니다.
   직접 입력한 주제로 준비할 때도 연관 검색어를 새로 조회하고, 이전 주제의 검색어는 제외합니다.
   순위는 검색어의 질문 의도·출처 수·설명용 이미지 적합성을 반영한 예상 관심 점수이며 실측 CTR이 아닙니다.
   CLI 탭의 **스포츠·사망 차단어**에서 쉼표·줄바꿈으로 목록을 수정하고 저장할 수 있습니다.
   검색어 또는 어느 출처의 연관어에든 차단어가 포함되면 주제 전체를 제외합니다. 수동 준비도
   같은 검사를 통과해야 합니다. 대소문자·띄어쓰기 차이는 무시하며, 차단한 단어를 로그에 남깁니다.
   `경기`는 지역·경제 표현에도 포함될 수 있습니다. 필요한 경우 목록에서 수정하세요.
   CLI 의미 검수에서도 스포츠·사망 관련 주제를 거절합니다.
3. 사용자가 제공한 4천자 이상 블로그 글쓰기 프롬프트가 기본으로 들어 있습니다. 내용을 자유롭게
   수정해 저장할 수 있습니다. **새 이름으로 저장**하면 여러 프롬프트를
   선택할 수 있습니다. 실행 단계는 1~4개이며 기본값은 ChatGPT → Claude → Antigravity → ChatGPT입니다.
   동일 CLI를 여러 번 선택할 수 있고, 순서·선택한 프롬프트·검수 방식·모델·발행 설정은 재실행해도 유지됩니다.
4. **글·이미지 준비**를 누릅니다. 질문에 답하는 제목과 정확히 8개 내용 구역을 작성하고 지정한 CLI 순서로
   사실·출처·한국어 문체를 검수합니다. 검수 방식은 단계별 교차 검수, 마지막 CLI 집중 검수,
   선택 CLI 모두 검수 중 고릅니다. 소제목에는 네이버의 따옴표·버티컬 라인·말풍선·라인&따옴표·포스트잇·프레임
   인용구 6종을 무작위로 적용합니다. 여섯 모양을 섞어 사용하며 선택한 모양은 원고에 저장해 재시도에도 유지합니다.
   글자 밑줄은 넣지 않습니다. 네이버 인용구 내부는 배경색을 지원하지 않아 사용자 선택대로 인용구 모양을 우선합니다.
   중요한 내용은 굵게, 핵심 단어는 서로 다른 진한 글자색으로 강조합니다. 아주 중요한 본문 문장만 1~3개 골라
   옅은 노랑·녹색·파랑·분홍·주황·민트 배경색을 무작위로 적용합니다. 색상도 저장되며 같은 단어는 같은 색입니다.
   Markdown이나 HTML을 본문에 넣지 않고 네이버가 저장하는 실제 서식을 사용합니다.
   출처와 URL은 공개 본문에 출력하지 않습니다. 검증용 출처와 검수 결과는 실행 자료 폴더에 남습니다.
5. 구역별 실사 스타일 이미지 후보를 Antigravity 4장, ChatGPT 4장 생성합니다. 실제 생성 도구의 성공과 새 파일을
   확인한 뒤 이미지 자체를 CLI에 전달해 글자·로고·워터마크·왜곡·관련성·품질·중복을 검사합니다.
   인물이 등장하면 현대 한국의 생활 맥락 속 가상의 한국인 성인으로 요청합니다. 자연광, 35mm·50mm 렌즈 느낌,
   자연스러운 피부·옷감 질감과 **아주 약한 미세 필름 그레인**을 사용하며 거친 노이즈·과한 보정을 피합니다.
   첫 사진에만 핵심 키워드를 포함한 짧은 한글 후킹 문구를 한글 글꼴로 배치합니다. 실제 최종 파일을 CLI가 보고
   문구 정확성·가독성·추가 글자 유무까지 검수합니다. 나머지 사진은 글자·숫자가 없어야 합니다.
   첫 표지를 포함해 검수에 통과한 서로 다른 생성 이미지 6장을 선정합니다. 표지 검수나 8장 생성·6장 선정이 실패하면 발행하지 않습니다.
   이전 규칙으로 생성한 이미지는 새 이미지 규칙의 완료 결과로 재사용하지 않습니다.
   업로드용 JPEG 사본은 긴 변을 최소 2048픽셀로 보정하고 EXIF·XMP·생성 도구 등 메타데이터를 제거합니다.
   원본과 내부 생성·검수 기록은 유지합니다. 확대가 원본에 없는 세부 정보를 복구하지는 않습니다.
6. Google 옵션을 켜면 검색 결과의 첫 두 유효 미리보기에서 이미지 요소만 캡처해 해상도와 선명도를
   보정합니다. 원본 페이지의 재사용 조건과 시각 검수가 모두 확인된 경우에만 최대 2장을 추가합니다.
   현재 자동으로 확인하는 원본은 Wikimedia Commons의 파일별 CC0·공유영역 표시입니다.
   공개 출처를 표시하지 않는 글쓰기 설정에 맞춰 출처 표시 의무가 있는 후보는 제외합니다.
   화면에서 글자를 자르거나 크기를 키우는 것만으로 저작권 문제가 해결되지는 않습니다.
7. 준비된 글을 확인한 후 **준비된 글 네이버에 입력·실행**을 누릅니다. 완료 후 설정이 **자동 발행**이면
   웨일 글쓰기에서 제목·8문단·사진을 입력하고 사진의 문단 위치와 내용을 확인한 뒤 발행합니다.
   **임시저장까지만**이면 임시저장 버튼을 누르고 저장 완료 표시를 확인합니다.
   **편집기에 입력만**이면 편집기에 배치하고 종료합니다. 입력만은 임시저장이나 발행을 뜻하지 않습니다.

상단 **반복 자동화 시작**은 주제 선정부터 지정한 완료 동작까지 반복합니다. 기본 완료 동작은
사용자 요청에 맞춰 **자동 발행**입니다. 간격은 1시간 또는 2시간이며, 시작 시 첫 회차를 실행한 뒤
시작 시각을 기준으로 반복합니다. 이전 회차가 길어지면 놓친 시각은 건너뛰고 작업을 겹쳐 실행하지 않습니다.
**중지**는 현재 CLI 요청, 후보 재시도와 이후 단계를 중단합니다. 중지 뒤 도착한 조회·로그인 결과가
자동화를 다시 시작하지 않습니다. 자동 시작 경로의 일반 오류는 상태 표시줄·로그에 남기며 모달 창을 띄우지 않습니다.
이미 제출한 발행 요청은 취소할 수 없습니다.

한 회차에서는 순위를 한 번 계산하고 **첫 시도를 포함해 최대 3개 후보**를 차례로 검토합니다.
선정 거절이나 원고·이미지 준비 실패 시 해당 주제를 이번 회차에서 제외하고 다음 후보로 이동합니다.
발행·임시저장 단계에 진입한 뒤 실패하면 즉시 다른 후보로 재시도하지 않고 다음 예약 회차로 넘깁니다.
CLI 로그인·설치 문제는 후보 재시도를 소모하지 않고 자동화를 멈춰 로그인 안내로 연결합니다.

발행을 확인한 키워드는 실시간 후보와 저장된 키워드에서 제거하고, 다음 회차에는 다른 키워드를 사용합니다.
이력은 재실행해도 유지됩니다. 임시저장·입력만·실패는 발행 완료로 처리하지 않으며,
제출 결과가 불확실한 키워드는 확인 전까지 자동 선정에서 보류합니다.

## 저장 위치와 검증

- 설정·프롬프트: `%LOCALAPPDATA%/PictureCleanerPC/settings.json`
- 원고·이미지·검수 기록: `%LOCALAPPDATA%/PictureCleanerPC/blog-runs/`
- CLI 기능 상태: `%LOCALAPPDATA%/PictureCleanerPC/blog-cli-capabilities.json`
- 최근 회차 후보 준비 실패 기록: `%LOCALAPPDATA%/PictureCleanerPC/last-cycle-attempts.json`
- 발행 결과·미확인 제출 기록: `%LOCALAPPDATA%/PictureCleanerPC/publication_receipts/`
- 발행 완료·선정 보류 키워드: `%LOCALAPPDATA%/PictureCleanerPC/published-topic-history.json`
- 댓글·공감 이력: `%LOCALAPPDATA%/PictureCleanerPC/naver_comment_history.json`

AI API 클라이언트와 API 키 입력창을 제거했습니다. 로그인된 CLI 자식 프로세스만 사용하며,
AI API 키 환경변수도 해당 자식 프로세스에 전달하지 않습니다. CLI 계정의 이용 한도는 적용됩니다.
기존 검색어 수집과 RSS 등 공개 웹 데이터 조회는 계속 사용합니다.
Antigravity 출처 검증은 요청별 임시 프로젝트에서 필요한 공개 도메인 읽기만 허용하며,
전역 설정과 기존 프로젝트는 변경하지 않습니다.

발행 성공은 네이버 게시물 URL과 표시된 제목을 확인한 경우에만 기록합니다. 발행 응답이 불확실하면
재실행해도 동일 원고를 자동 재제출하지 않습니다. 기록의 안내와 웨일 화면에서 상태를 확인하세요.
사진이 검수 후 변경되거나 문단/검수 기록이 달라진 경우에도 발행을 중단합니다.

## 개발

`python -m unittest test_core test_blog_controls test_blog_workflow test_blog_cli_bridge test_blog_topic_history test_image_delivery test_naver_publish test_comment_browser test_blog_unattended test_blog_visual_style`

이전 CLI 블로그 버전에서 사용자 요청에 따라 실제 네이버 글
`https://blog.naver.com/macdcross/224409562234`를 발행하고 다시 열어 8개 본문 구역,
사진 6장의 식별자·문단 위치, 굵은 글씨, 발행한 `기부` 키워드의 다음 후보 제외를 확인했습니다.
이때 Claude는 미로그인 상태라 실제 원고 검수는 ChatGPT → Antigravity → ChatGPT로 진행했으며,
프로그램의 기본 4단계 설정은 유지했습니다. 검증 결과는 `qa-cli-blog/live-publication-result.json`에 있습니다.

2026-09-12 무인 실행 검증에서는 차단어에 해당하는 실시간 후보가 제외되는 것을 확인했고,
첫 후보의 원고 검수 실패 후 다음 후보로 자동 이동했습니다. 추가된 이미지·서식 요구를 적용하기 위해
그 실행은 중단했습니다. 이후 검증은 승인된 기존 원고의 글을 재사용하여 새 이미지와 서식, 임시저장을 확인했습니다.
새 회차의 공개 발행 성공이나 새로운 키워드 소비로 기록하지 않습니다.
네이티브 인용구 6종과 중요 문장·단어 서식의 실제 문서/화면 검증은 `qa-cli-blog/native-visual-check.json`,
ChatGPT·Antigravity의 새 카메라 이미지 생성·상호 검수는 `qa-cli-blog/camera-sample-check.json`에 있습니다.
새 규칙으로 Antigravity 4장·ChatGPT 4장을 생성하고 실제 파일 검수 후 6장을 골라 네이버에 임시저장했습니다.
21:37에 저장한 글을 임시글 목록에서 다시 불러와 8개 구역·6개 사진의 식별자와 위치·인용구·굵게·글자색·문장 배경색이
유지되는 것까지 확인했습니다. 결과는 `qa-cli-blog/camera-article-draft-result.json`과
`qa-cli-blog/camera-draft-reload-result.json`에 있습니다. 최신 회귀 테스트는 310개 모두 통과했습니다.

브라우저 회귀 테스트는 별도 프로필의 로컬 HTML을 사용합니다. 실행 계정의 실제 댓글·게시물을 만들지 않습니다.
다시 빌드하려면 PowerShell에서 `./build.ps1`을 실행합니다. `PictureCleanerPC.spec`에 포함된 네이버 드라이버는
브라우저 버전에 맞춰 실행 시 확인됩니다. 결과 사진은 원본을 보존하며, 원본 삭제 옵션은 휴지통 이동입니다.
