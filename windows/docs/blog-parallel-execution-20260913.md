# Blog 병렬 실행 개선 — 2026-09-13

## 실행 흐름

주제 선정 뒤 원고 작성과 Google 영어 이미지 검색·캡처를 함께 시작한다. 초고의 8개 구역과 이미지 프롬프트를 검증하고 저장한 다음, 후속 글 검수와 이미지 생성을 겹쳐 실행한다. Google 후보는 검색 결과 순서대로 확인하고, 사용 조건이 확인된 실제 사진에 대해 글자·로고·워터마크 사전 검사를 진행한다.

ChatGPT와 Antigravity는 각각 한 개 작업만 실행한다. 다른 제공자의 이미지 생성은 동시에 진행하며, 같은 제공자에 대기 중인 글 작성·최종 검수는 새 이미지 작업보다 먼저 실행한다. 이미 실행 중인 정상 CLI 요청을 우선순위 변경만으로 중단하지 않는다.

초고로 생성한 사진은 선행 작업 결과일 뿐 승인된 사진이 아니다. 최종 본문의 해당 구역과 실제 사진을 대조하여 검수한다. 썸네일 후킹 문구가 바뀌면 표지 슬롯을 새 문구로 다시 생성하고 다른 사진은 보존한다. 탈락한 사진은 기존 설정대로 최대 2회만 재생성한다.

## 시간과 예약

자동 회차마다 monotonic 시계 기준 50분 예산 하나를 생성한다. 내부 재시도는 같은 예산을 사용하고, 다음 예약 회차에서만 새 예산을 받는다. 실행 객체는 설정이나 원고 지문에 직렬화하지 않는다.

- 일반 작성·이미지 생성·Google 준비: 마지막 10분을 남기고 새 요청을 중단한다.
- 최종 사실·문체·사진 검수: 마지막 5분을 남긴다.
- 완료한 원고·사진·검수와 유료 생성 시도 기록은 취소·실패 때도 저장한다.
- 1시간 예약은 기존 시작 기준 간격을 유지한다. 오래 걸린 회차를 만회하려고 중복 회차를 동시에 실행하지 않는다.

50분은 프로그램의 공통 시간 예산이다. 외부 CLI·로그인·네이버 통신이 실패한 상황에서 미완료 원고를 성공으로 표시하거나 중복 발행하지 않는다. 최종 제출 이후 응답이 불확실하면 영수증을 유지하여 재제출을 막는다.

## 저장과 브라우저 소유권

이미지 작업자는 각자의 생성 폴더만 사용하고, 조정 스레드 하나가 manifest와 생성 시도 수를 기록한다. 글 검수 실패나 취소 시 모든 완료 결과를 회수한 뒤 종료하여 늦은 작업이 기존 기록을 덮어쓰지 않게 한다.

Google 작업자는 별도 체크포인트에 영어 검색어, 사진 지문, 사전 검수 결과를 저장한다. 본문 작업자는 반환된 후보를 한 번만 합친다. Google 브라우저 작업이 끝나기 전에는 네이버 입력·발행을 시작하지 않는다. 제한 시간 안에 종료하지 못한 작업이 있으면 브라우저 사용을 막고 기존 취소 신호를 유지한다.

## 실제 발행 복구

09:12:52에 기존 공휴일 원고를 제출하고 네이버 발행을 확인했다. 제목·본문 8구역·사진 6장과 서식이 검증됐고, 업로드 사진 6장의 원본 SHA256도 로컬 검수 사진과 모두 일치했다.

발행 설정 창이 이미 열려 있으면 헤더 버튼을 다시 누르지 않는다. 정상 클릭 뒤 설정 창이 나타나지 않을 때만 기존 안내창 확인, 본문 전체 재검사, 같은 헤더 버튼 확인을 거쳐 설정 열기 동작을 한 번 보완한다. 최종 제출은 기존대로 영수증을 먼저 기록하고 단 한 번 클릭한다.

검증된 게시글: https://blog.naver.com/macdcross/224409924187

## 검증 범위

회귀 검사는 제공자별 동시 실행 수, 글 검수 우선순위, 초고 생성과 후속 검수의 실제 겹침, 최종 본문 기준 이미지 검수, 재개 시 생성·검수 재사용, 예산 만료 시 기록 보존, CLI 입력/출력 정체의 제한 시간, Google 단독 브라우저 사용, 시간 초과 뒤 1시간 예약 유지, 발행 설정 복구와 단일 제출을 포함한다.

실제 새 회차의 50분 이내 완료 여부는 실행 로그의 시간으로 추가 확인한다. 단위 테스트의 통과만으로 외부 서비스 완료 시간을 보장하지 않는다.

## 첫 제목과 마지막 제목의 핵심 통합

첫 제목의 궁금증형 훅과 마지막 SEO 제목의 설명을 함께 고려해, 실제 연관 검색어 1~2개와 독자가 얻을 정보를 첫 제목에 담는다. 40~65자를 권장하고 기존 최대 70자 제한은 유지한다. 같은 연도·주제어를 반복하거나 두 제목을 단순 연결하지 않는다. 마지막 제목은 같은 검색 의도를 다른 표현·정리 관점으로 설명하며 `뜻과 의미`로 끝낸다. 썸네일의 짧은 문구는 별도 규칙을 유지한다.

짧은 키워드 나열, 연도 반복, 첫·마지막 제목 사이의 핵심어 연결을 편집 보완 사항으로 기록한다. 글자 수만으로 발행을 차단하지 않는다. 제목 지적만 있을 때는 모델에 제목 필드만 수정하도록 요청하고 본문·수치·출처·검색 의도·이미지 메타데이터는 보존한다. 제목에 새로운 수치를 만들어 넣지 못하며, 바뀐 제목은 최종 원고와 함께 독립 검수를 거친다. 검증이 끝난 기존 발행 영수증이나 승인 원고를 새 제목 기준으로 소급 재작성하지 않는다.

실행 중이던 새 회차에서는 09:26:18부터 Google 검색과 ChatGPT 원고 작성이 실제로 병행됐다. 사용자의 제목 규칙 변경을 반영하기 위해 초고 완성 전 정상 중지했다. 완료한 자료와 회차 재개 정보를 유지하고 새 버전에서 이어간다.
